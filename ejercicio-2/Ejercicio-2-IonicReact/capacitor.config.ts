import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ejercicio-2-IonicReact',
  webDir: 'dist'
};

export default config;
